#include <stdio.h>

main (void){
	int idade=25;
	/*O %d exibe a variável em forma de caracter na posição que você especificou, mesmo que ela esteja no fim do print.
	Por isso, o print não imprime nada após a declaração das variáveis*/
		printf("Eu tenho %d anos",idade);
}
